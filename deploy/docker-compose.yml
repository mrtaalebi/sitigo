version: '3.3'

services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./files/static/:/sitigo/static/
      - ./files/media/:/sitigo/media/
      - ./log/nginx/:/var/log/nginx/
      - ./config/nginx/sites-available/:/etc/nginx/sites-available/
      - ./config/nginx/sites-enabled/:/etc/nginx/sites-enabled/
      - ./config/nginx/nginx.conf:/etc/nginx/nginx.conf
      - /etc/letsencrypt/live/igo-official.ir/:/etc/letsencrypt/live/igo-official.ir/
    depends_on:
      - web
  web:
    build:
      context: ..
      dockerfile: ./deploy/Dockerfile
    container_name: web
    restart: always
    command: bash -c "export DJANGO_SETTINGS_MODULE=igo.settings.production && python3 manage.py migrate && python3 manage.py compilemessages && python3 manage.py collectstatic <<< yes && gunicorn -b 0.0.0.0:8000 --workers=4 igo.wsgi"
    volumes:
      - ./static/:/app/static/
      - ./media/:/app/media/
    depends_on:
      - db
    expose:
      - 8000
  db:
    image: postgres:latest
    container_name: db
    restart: always
    environment:
      - POSTGRES_PASSWORD=wowpass
